# Release notes — v1.1.0

Краткая сводка для пользователей и администраторов. Полный список изменений — [CHANGELOG.md](../CHANGELOG.md).

---

## Демо

Попробовать без установки: **[buhgalter-demo.kai-zer.ru](https://buhgalter-demo.kai-zer.ru/)**

| Логин | Пароль      |
| ----- | ----------- |
| demo  | demo_1_demo |

---

## Сброс пароля (через администратора)

Сценарий для self-hosted:

1. Пользователь на `/login` → **«Запросить сброс пароля»** (логин).
2. Администратор видит баннер и список запросов; переходит в **Админка → Пользователи**.
3. Админ задаёт новый пароль (`PUT /api/v1/admin/users/{id}/password`); сессии пользователя сбрасываются.

API:

| Метод | Путь | Кто |
|-------|------|-----|
| `POST` | `/api/v1/auth/request-password-reset` | любой (rate limit по IP) |
| `GET` | `/api/v1/admin/password-reset-requests` | админ |
| `POST` | `/api/v1/admin/password-reset-requests/{id}/ack` | админ |
| `PUT` | `/api/v1/admin/users/{id}/password` | админ |

Миграция: `023_password_reset_requests.sql`.

Подробнее: [api/authentication.md](api/authentication.md).

---

## Кредиты

### График с процентами

- Ровно `term_months` платежей (исправлен укороченный график, например 22 вместо 36).
- Платежи 1…N−1 — равный аннуитет; последний закрывает остаток тела и проценты за период.
- При старте сервера активные кредиты с неполным графиком **дополняются** (`RepairShortSchedules`, миграция-маркер `020`).

### Редактирование графика

- На странице кредита → блок «Неоплаченные» → изменить суммы будущих `scheduled`-платежей.
- `PATCH /api/v1/credits/{id}/schedule` — только неоплаченные строки; связанные операции `kind=future` обновляются.
- **Оплаченные** платежи (`is_applied`) нельзя редактировать; в группе «Оплаченные» — подсказка. Удаление — отдельное подтверждение с предупреждением о возможной просрочке.
- Операции, привязанные к платежу (`credit_payment_linked` в API), нельзя менять через форму операции — только на странице кредита.

### Кредит задним числом

- Флаг **«Уже платил по графику»** при создании — прошлые платежи учитываются в `paid_amount` без влияния на баланс (по умолчанию).
- **«Списан со счёта»** — для хвоста последних прошлых платежей (`retroactive_debit_count`): создаётся расход на счёте списания, платёж попадает в статистику.

Подробнее: [ui-credits.md](ui-credits.md).

---

## Переводы и категории

- При переводе между счетами — опциональное поле **комиссии**; сумма уходит расходом в системную категорию **«Комиссия»** (иконка `percent`).
- **Редактирование перевода** из списка операций (страница счёта, `/transactions`): `PUT /api/v1/transfers/{group_id}`.
- Системные категории: «Комиссия», «Кредиты», «Долги» (income/expense) — seed при создании пользователя и backfill при старте.
- При ручной сортировке категорий системные всегда в конце; системную нельзя сделать главной.
- В фильтрах и статистике категории с одинаковым именем различаются суффиксами **(Доход)** / **(Расход)**.

Подробнее: [categories-and-icons.md](categories-and-icons.md), [transactions-display.md](transactions-display.md).

---

## Статистика и фильтры

- `/stats`, `/transactions`, страница счёта: фильтры **только по дате** (без времени); границы суток — в часовом поясе пользователя.
- `/stats`: фильтры применяются автоматически при изменении; поиск по описанию в drill-down.
- Сводка на странице счёта: входящие переводы в «Доходе», исходящие в «Расходе» (как баланс).
- Фактические суммы согласованы со списком операций: прошедшие плановые операции активируются при чтении; платежи по кредиту учитываются при `exclude_from_stats=0`.

---

## Интерфейс

- **Мобильные браузеры:** touch-targets 44px, карточки вместо широких таблиц, bottom sheet у модалок, горизонтальный скролл вкладок, компактное меню в шапке (пункты «Счета», закрытие при переходе, корректный z-index).
- Форма нового кредита сбрасывается при повторном открытии без перезагрузки страницы.
- В админке внизу — кнопка поддержки автора (донат) и ссылка на GitHub.
- Переключатели (`ToggleSwitch`) вместо чекбоксов: кредиты, долги, уведомления, импорт, фильтры, админка.
- Фильтр категорий по типу операции (доход/расход) на списках операций и в статистике.
- Поле суммы (`MoneyInput`): курсор не прыгает при правке в середине числа.
- На странице счёта — иконка «Архивировать» в ряду действий.

Подробнее: [ui-navigation.md](ui-navigation.md), [ui-stable-layout.md](ui-stable-layout.md).

---

## Roadmap

Черновик: [сканер чеков](../roadmap/receipt-scanner.md) — расход из фото или QR чека.

---

## Обновление с v1.0.0

1. Сделайте бэкап БД ([README — Бэкапы](../README.md#бэкапы)).
2. Замените бинарник или образ Docker (`ghcr.io/kai-zer-ru/buhgalter:v1.1.0` / `latest`).
3. При старте применятся миграции `020`, `023`; для существующих пользователей создастся категория «Комиссия», при необходимости дополнятся графики кредитов.

Версию можно проверить в **Админка → Диагностика**.

---

## API

OpenAPI **1.1.0**: [api/openapi.yaml](api/openapi.yaml), интерактивно — `/docs` на работающем сервере.
