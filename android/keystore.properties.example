# Copy to keystore.properties (gitignored) for local release signing.
# CI uses ANDROID_KEYSTORE_* environment variables instead.
storeFile=/absolute/path/to/buhgalter-release.jks
storePassword=change-me
keyAlias=buhgalter
keyPassword=change-me
